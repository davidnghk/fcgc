<%# Place any links for the RIGHT side of the nav bar here. Be sure to add class: "nav-link" to each item for proper spacing. %>

<%= nav_link_to "Pricing", pricing_path if Jumpstart.config.payments_enabled? && not_subscribed? %>
<%= nav_link_to content_tag(:span, "What's New"), announcements_path, class: [ unread_announcements_class(current_user)] %>
