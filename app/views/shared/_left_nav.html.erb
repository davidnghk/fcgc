<%# Place any links for the LEFT side of the nav bar here. Be sure to add class: "nav-link" to each item for proper spacing. %>
<% if current_team.owner == current_user %>
	<%= nav_link_to t('works'), works_path, data: { turbolinks: false } %>
	<%= nav_link_to t('customers'), customers_path, data: { turbolinks: false } %>
<% end %>
<%#= nav_link_to t('tasks'), tasks_path, data: { turbolinks: false } %>
<%= nav_link_to t('documents'), documents_path, data: { turbolinks: false } %>
<%= nav_link_to t('new_document'), new_document_path, data: { turbolinks: false } %>

<a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= t('tasks')%><b class="caret"></b></a>
	<ul class="dropdown-menu">
	  <li><%= link_to t('new_task'), new_task_path, data: { turbolinks: false } %> </li> 
	  <li><%= link_to t('tasks'), tasks_path, data: { turbolinks: false } %> </li>
	</ul>
<%= nav_link_to "Discussions", discussions_path, class: 'nav-link' %>
