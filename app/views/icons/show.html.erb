<div class="container mx-auto my-8 px-4">
  <div class="max-w-2xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3"><%= link_to 'Icons', icons_path %> > Icon #<%= @icon.id %></h1>
      <%= link_to 'Edit', edit_icon_path(@icon), class: "btn btn-link" %>
    </div>

    <div class="bg-white rounded shadow p-8">
            <div class="mb-4">
        <h5>Code</h5>
        <%= @icon.code %>
      </div>
            <div class="mb-4">
        <h5>Name</h5>
        <%= @icon.name %>
      </div>
            <div class="mb-4">
        <h5>Local name</h5>
        <%= @icon.local_name %>
      </div>
          </div>
  </div>
</div>
